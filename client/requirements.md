## Packages
date-fns | Date formatting for run logs
framer-motion | Smooth animations for page transitions and elements
lucide-react | High-quality icons for the UI

## Notes
The application uses a custom polling mechanism in `use-runs.ts` to automatically refresh the dashboard when any run is in the 'running' state.
